# Sanity CMS — create a free project at https://www.sanity.io/manage
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_TOKEN=

# SMTP for the Contact & Booking forms (e.g. Namecheap Private Email, Gmail app password, etc.)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
CONTACT_TO_EMAIL=tangtriduc@triduccar.media

# Port used when running as a standalone Node.js process (Namecheap cPanel "Setup Node.js App"
# sets this automatically — do not hardcode it in production).
PORT=3000
