# Founder photo

Drop the founder portrait here as `tang-tri-duc.jpg` (portrait orientation,
at least 1200×1500px, JPG or WebP). The site automatically uses it once
present — `components/FounderPortrait.tsx` falls back to a "TTĐ" monogram
card until the file exists, so nothing breaks in the meantime.
